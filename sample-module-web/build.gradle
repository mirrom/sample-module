apply plugin: 'java'

compileJava {
    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'
}

dependencies {
    compileOnly group: 'com.liferay.portal', name: 'release.dxp.api'
    
    compileOnly project(':modules:sample-module:sample-module-api')
}
